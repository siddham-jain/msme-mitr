// Export all contexts from a single entry point
export { AuthProvider, useAuth } from './AuthContext'
export { ThemeProvider, useTheme } from './ThemeContext'
export type { UserProfile, UserProfileUpdate } from '@/types/database'
